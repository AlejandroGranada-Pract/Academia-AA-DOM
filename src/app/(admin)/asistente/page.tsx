import { EnConstruccion } from "@/components/layout/EnConstruccion";

export default function AsistentePage() {
  return (
    <EnConstruccion
      title="Asistente IA"
      nota="Creación de cursos asistida por IA (Claude). Genera estructura, lecciones y preguntas como borrador. (Fase 2)"
    />
  );
}
